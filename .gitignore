*.docker
.idea
bin
/build/
.env
.env.local
/infra/
.inbox.log
.afk.log

# Render output (model I: rendered manifests are never committed).
testbeds/*/k8s

# Per-testbed git roots the smoke harness creates (platform requires a repo root; it
# never runs `git init` itself).
testbeds/*/.git

# ACE-managed — do not edit this block.
# Managed by ACE. See https://github.com/ace-rs/ace
.agents/agents
.agents/commands
.agents/rules
.agents/skills
.claude/agents
.claude/commands
.claude/rules
.claude/skills
.opencode/agents
.opencode/commands
.opencode/rules
.opencode/skills
ace.local.toml
# end ACE
