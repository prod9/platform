<!-- not spec/decision because: session handoff breadcrumb, not durable design -->

# Resume — 2026-07-11 (Framework refactor locked; terminology Pass 1 done, docs-audit Pass 2 next)

Entry point for the next session. Supersedes [`2026-07-10-resume.md`](2026-07-10-resume.md).
`main`/HEAD is at **`e3fa5c6`** ("Checkpoint in-progress project-centric refactor") — the
**reference point**. Everything since is **uncommitted** (operator wants one reviewable diff
vs `e3fa5c6`; **no commits this session**).

## FIRST — next session starts here

**Pass 2 (spec/docs audit) is DONE** and the **migration slices are IMPLEMENTED** (same
session) — see the status blocks in
[`2026-07-11-terminology-docs-audit-plan.md`](2026-07-11-terminology-docs-audit-plan.md)
and [`2026-07-08-builder-refactor-plan.md`](2026-07-08-builder-refactor-plan.md). All
seven slices + the terminology addendum are in the tree (uncommitted, ~105 files vs
`e3fa5c6`), build/vet/tests green, fresh-eyes audit agent ran and its findings were
fixed. **Next: the operator reviews the diff and runs `./test.sh`** — expect several
CHANGED lines (not just the one defaults-basics creds line: init lost its picker/prompts,
scaffold writes `framework =` + cue.mod, plan output changed shape); review each and
`--commit`. buildlog event methods got doc comments; the `builder`-key deprecation note
goes through `buildlog.Config` (Warn — visible), per the consolidate-by-construction log
philosophy.

**Terminology Pass 1 is DONE (this session):** 11 naming rulings collected 2-by-2, frozen in
the [terminology lexicon ADR](../decisions/2026-07-11-terminology-lexicon.md) (one word, one
concept — component/bump, strategy, render/Changelog, interpreter, framework toml key with
deprecated `builder` alias, Class() deleted, Arch, vestigial deletions, pinned
non-collisions). All spec/ word-edits applied; straggler grep clean. Intentional leftovers:
frameworks.md's legacy-alias mention (is the ruling), engine.md's `builder/unit.go` link
(code-true; Pass 2 annotates).

**Read these three to reload the locked shape** (they teach the target, not the fork's old
shape): [`spec/frameworks.md`](../spec/frameworks.md), [`spec/scaffolding.md`](../spec/scaffolding.md),
and [ADR `2026-07-11-baseline-dissolves-into-infra-framework`](../decisions/2026-07-11-baseline-dissolves-into-infra-framework.md).

## The locked shape (one paragraph)

A `Framework` is the **sole owner of a project type** — Discover, Scaffold (RICH,
per-framework), Build. **One** files/templating mechanism: `framework/scaffold/`. `baseline/`
**dissolves** (templating → `framework/scaffold/`; embedded infra files + pins + routing → into
the `Infra` framework). Top-level `scaffold/` **dissolves** (mechanism → `framework/scaffold/`;
discover → `framework/`; orchestration → `cmd/init`). **`IsInfra()` and any app-vs-infra
branch are ELIMINATED** — Infra.Scaffold just contributes more (its baseline files,
`strategy="latest"` seed, create-repo need). `builder/` renames to `framework/`. Strategy is a
scaffold-time seed the framework contributes, never a runtime method. Registry creds are
defaulted placeholders (committed CUE, operator hand-edits), never prompted; the picker is gone.

## What's landed in the tree (uncommitted, vs `e3fa5c6`)

The **complete locked shape** — all seven migration slices + the terminology addendum
(`git add -A -N && git diff e3fa5c6` shows the full diff, ~105 files):

- `framework/` (renamed from `builder/`): contract `Name/Layout/Discover/Scaffold/Build`
  (`Class`/`IsInfra` deleted); `framework/scaffold/` is the one mechanism
  (`scaffold.Spec`/`File`/`Data`, `Resolve`); the Infra framework embeds the baseline
  (`framework/baseline.go` + `framework/baseline/` files) and its rich `Scaffold`
  contributes everything (files, pins, `strategy="latest"`, `NeedsGitRepo`, greenfield
  cue.mod as `.tmpl`).
- `baseline/` and top-level `scaffold/` packages deleted; orchestration lives in
  `cmd/init*` (plan-then-apply, framework-set git gate, dir check before discovery).
- Renames: toml `framework` key (+ deprecated `builder` read-alias w/ Config-level note),
  `FindFramework`, `BuildUnit.Framework`/`.Arch`, releases `Bump*` + `Changelog()`, dsl
  `interpreter`; vestigial `deploy` exclude + `environments` key deleted; testbeds +
  repo toml on the canonical key. Specs' code-true mentions updated.

A fresh-eyes audit agent reviewed the whole diff: no correctness regressions, full
design conformance; its lexicon/naming findings were fixed in-tree.

## Constraints / owed

- **No commits** until the operator reviews the diff.
- **`./test.sh` (docker) is owed** — the real drift gate. Expect **several** `CHANGED`
  lines now, by design: the original defaults-basics creds comment, plus init's changed
  prompt set/plan output, scaffolded `framework =` key, cue.mod in infra init. Review
  each, then `./test.sh --commit`. None should be a behavior regression.
- Dead surface removed (operator-authorized): `framework.All()`, the `ImagePrefix`
  prompt/field, the launcher's no-op template pass. Remaining known wart:
  `platform.template` carries a stale hardcoded version (content, not code).

## Pending school changes (for `ace-school`, still not done — carried from 07-10)

- **`go-coding`** — prefer Go 1.21+ native `GOTOOLCHAIN=go<version>` over the legacy
  `golang.org/dl/go<v>@latest` shim (no per-invocation proxy hit; caches under
  `golang.org/toolchain`).
- **`ace-docs`** — when pruning `scratch/`/`notes/`, retain files a frozen decision cites as
  provenance (don't dangle frozen links); the `scratch/` toll line applies to new files only.

## Session note

`/realign` is active on: *re-derive from first principles at the level of decomposition/
interface — don't patch variations on the first idea to thread the operator's corrections.*
This session's whole design arc (builder → the locked Framework shape) came from the operator
correcting exactly that failure repeatedly; the docs now capture the endpoint so it needn't
recur.
