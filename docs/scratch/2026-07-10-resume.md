<!-- not spec/decision because: session handoff breadcrumb, not durable design -->

# Resume — 2026-07-10 (docs consolidation + ace-docs re-file landed & pushed)

Entry point for the next session. Supersedes
[`2026-07-09-resume.md`](2026-07-09-resume.md). `main` is at `fee36a9`, **up to date with
`gh`** — everything this thread is pushed.

## FIRST — next session starts here

The working tree is **Bucket B only** again — this thread touched docs exclusively; the
builder-refactor WIP is untouched.

- **Bucket B — the Project-Centric Builder Refactor — is the remaining real work.**
  Uncommitted WIP across `builder/*.go`, `scaffold/plan.go`,
  `baseline/files/apps-flux.platform`. Plan + Law (no commits until its final
  all-encompassing review):
  [`2026-07-08-builder-refactor-plan.md`](2026-07-08-builder-refactor-plan.md).

## Landed this thread (docs only, all pushed)

- **`116e2d3` — design-record consolidation.** Full spec run: 5 new specs (`builders`,
  `engine`, `releases`, `scaffold-baseline`, `platform-server`). Promotions out of notes:
  GitHub-App zero-RBAC ADR, build-cache troubleshooting guide, NGF install reference,
  stays-private ADR + before-going-public guide. Reconciled stale specs (`ops`/`bootstrap`
  → `init`/`render`/`scaffold`, de-sliced the DSL build-plan, `clean` added). Pruned 15
  notes; kept 6 superseded notes cited by frozen ADRs/specs as provenance.
- **`fee36a9` — re-file into canonical ace-docs structure.** `reference/` → `vendor/`,
  `notes/` → `scratch/`, `migration.md` → `guides/` (all `git mv`, history preserved). Root
  README on the single routing gate; new `vendor/`/`scratch/` READMEs. No old folder or
  reference survives.

## Load-bearing conventions established this thread

- **Provenance beats prune.** When pruning `scratch/`, keep any file a frozen decision or
  spec cites as its evidentiary record — deleting it dangles a link in a frozen ADR, which
  we don't edit. The 6 retained notes are listed in `scratch/README.md`.
- **scratch/ toll line** governs *new* files; not retrofitted onto historical/disposable
  breadcrumbs.

## Parked / handoff

- **fx handoff** —
  [`2026-07-09-fx-handoff-slog-logvaluer-sink.md`](2026-07-09-fx-handoff-slog-logvaluer-sink.md):
  fxlog sinks don't resolve `LogValuer`/groups. **fx agent's remit**; platform's local
  handler stands until fx ships it.

## Pending school changes (for ace-school, not done here)

- **`ace-docs`** — when pruning `scratch/`/`notes/`, retain files cited by a frozen decision
  as provenance (don't delete and dangle frozen links); the `scratch/` toll line applies to
  new files only, not historical disposable ones.
- **`go-coding`** (carried from 07-09, still pending) — prefer Go 1.21+ native
  `GOTOOLCHAIN=go<version>` for pinning an exact toolchain over the legacy
  `golang.org/dl/go<v>@latest` shim — no per-invocation proxy hit; caches under
  `golang.org/toolchain` in the module cache.
