# Resume — 2026-07-09 (logging + clean + go-toolchain landed; only Bucket B left)

Entry point for the next session. Supersedes
[`2026-07-08-resume.md`](2026-07-08-resume.md). `main` is at `b2c292d`, **1 ahead of
`gh`** (`b2c292d` toolchain — unpushed; earlier commits are pushed).

## FIRST — next session starts here

Three slices landed and green this thread; the working tree is now **Bucket B only**.

1. **Push `main` → `gh`** when ready — carries `b2c292d` (the go-toolchain fix).
2. **Bucket B — the Project-Centric Builder Refactor — is the remaining work.** Uncommitted
   WIP across `builder/*.go`, `scaffold/plan.go`, `baseline/files/apps-flux.platform`. Plan +
   Law (no commits until its final all-encompassing review):
   [`2026-07-08-builder-refactor-plan.md`](2026-07-08-builder-refactor-plan.md). This is the
   next real task.

## Landed this thread (all green)

- **`4ce8ee4` — logging rework** (slog + tint; enriched ExecError errors). Settled, pushed.
- **`409f443` — `platform clean`** (prune local Dagger cache; pnpm cache-poison recovery).
  Pushed. **Do NOT re-propose `apk add nodejs`.**
- **`e6a7da2` — notes consolidation.** Pushed.
- **`b2c292d` — go toolchain via native GOTOOLCHAIN** (dropped the `golang.org/dl/go<v>@latest`
  shim + `/root/sdk` cache). Fixes the go build-speed regression — go beds now clear the 1m
  smoke gate. **Unpushed.** Landed as a surgical per-hunk commit, separate from the Bucket B
  WIP that shares `go_basic.go`/`go_workspace.go`. Details:
  [`2026-07-09-go-build-speed-regression.md`](2026-07-09-go-build-speed-regression.md).

## Test state (verified end of thread)

- `go test ./...` — green.
- `./test.sh` smoke — **UNCHANGED / green** (all beds, incl. all pnpm and both go beds).
  Nothing to re-record.

## Parked / handoff

- **fx handoff** —
  [`2026-07-09-fx-handoff-slog-logvaluer-sink.md`](2026-07-09-fx-handoff-slog-logvaluer-sink.md):
  fxlog sinks don't resolve `LogValuer`/groups. **fx agent's remit**; platform's local
  handler stands until fx ships it.

## Pending school change (for ace-school, not done here)

- `go-coding` skill could note: prefer Go 1.21+ native `GOTOOLCHAIN=go<version>` for pinning
  an exact toolchain over the legacy `golang.org/dl/go<v>@latest` shim — no per-invocation
  network proxy hit, toolchain caches in the module cache under `golang.org/toolchain`.
