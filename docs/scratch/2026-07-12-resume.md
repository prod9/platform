<!-- not spec/decision because: session handoff breadcrumb, not durable design -->

# Resume — 2026-07-12 (infra-is-a-framework excision SHIPPED + scratch collapse)

Entry point for the next session. Supersedes the prior resume breadcrumbs (since collapsed
into [`prior-art.md`](prior-art.md)). This session **shipped code + docs** — four commits on
`main`, **not pushed** (push waits for operator say-so). Tree is clean.

## What landed (4 commits, verified green, unpushed)

The "infra is just a framework, not a global category" relapse (5th recurrence) was fixed at
its **source** — the artifacts that re-seed it — not by re-attention. A 5-agent audit found
the code was already structurally clean (no `IsInfra` anywhere); the relapse was fed by prose,
naming, and the vestigial `[ops]` type.

- `c20cce1` **project: delete `[ops]`**. `Ops.Image/Tag/Ref`/`ErrNoOpsImage` were dead —
  publish already derives image per-module (`Module.ImageName`, `InferImageBase`) and tag from
  release strategy. Only `Ops.Vars` was live → moved to **top-level `[vars]`**. Renames:
  `InferOpsImage`→`InferImageBase`, `Data.OpsImage`→`ImageBase`, `MergeOpsVars`→`MergeVars`.
  Migrated the infra-basic testbed + `./infra` config off `[ops]`.
- `f5c7c0d` **docs: excise infra-as-category prose**. init help, publish/init_validate
  comments, tests.cue, CLAUDE.md (rewrote the stale `Ops` API block + subcommand rows),
  releases/frameworks/architecture/platform/manifest-patch specs; `[ops.vars]`→`[vars]` fleet-wide.
- `5a0b836` **framework: Discover dispatch lock**. New `discover_test.go` — table over every
  testbed through the one `Discover` loop proves Infra is one ordinary entry. A reintroduced
  `IsInfra` short-circuit now **fails the image build** (test-in-build gate). This is the
  durable guard against a 6th relapse.
- `3be47e5` **docs: collapse prior-art**. 7 superseded scratch design/plan files → one
  [`prior-art.md`](prior-art.md) digest; 10 inbound citations repointed; **−1490 lines**.
  Promoted the one uncaptured durable point (no-`api`-layer / `cli`-must-not-import-`srv`) →
  `spec/platform-server.md`. Deleted 3 stale uncited resume breadcrumbs.

## Also landed this session — `import_prefix` setting + CUE module-path decoupling

Grew out of a live bug the operator hit: `platform init` for an infra repo with a bare,
non-domain repository (`prod9/infra-new`) failed at **render** — `planSpecFiles` forced the CUE
module path equal to `repository`, and CUE demands a domain (dot) in the first path element. The
hyphen was a red herring; the missing domain was the cause.

- `431db71` **project: seed cue.mod module path from `import_prefix`, not repository.** New
  top-level `import_prefix` setting = the operator's CUE module namespace, a concern separate
  from the GitHub `repository` (both real infra repos already diverge: module `prodigy9.co` /
  `infra-basic.test` ≠ repo). Infra seeds the `example.com` placeholder (renders immediately,
  operator edits — empty-placeholder philosophy). Folded `Strategy`+`ImportPrefix` into
  `GenerateInfo`. ADR: `docs/decisions/2026-07-12-cue-module-path-via-import-prefix.md`. Name
  `import_prefix` chosen to dodge serving-domain / k8s-namespace / our-`[modules]` collisions.
- `8381d33` **tests: Infra Init smoke test.** Dedicated git-ignored `testbeds/infra-init/`
  (commits only `.gitignore`; init generates the baseline + a nested `.git` fresh, all ignored —
  smoke snapshots bytes into the lock; `ALWAYS_YES=1` auto-confirms). Inits with the bare
  `prod9/infra-new`, snapshots the generated `platform.toml` (proves module path from
  import_prefix, not repo), and renders clean (Q2 regression guard). Verified: the nested `.git`
  does **not** dirty the parent tree once `.gitignore` is tracked.

**Deferred (backlog, operator-approved):** give `import_prefix` render teeth —
`{import_prefix}/apps` instead of hardcoded `./apps` (see backlog item below).

## FIRST — next session

1. **Push** the unpushed commits once operator OKs (**11** as of 2026-07-12: the infra-excision
   set + `3ec1785` rolling rename + `431db71` import_prefix + `8381d33` infra-init smoke test +
   breadcrumb updates).
2. **Re-record smoke goldens**: `./test.sh --commit` (needs docker) — generated `platform.toml`
   now emits `[vars]` and the infra-basic testbed dropped `[ops]`, so expect CHANGED. Review
   the diff, then commit. **Do not skip** — the goldens are stale until re-recorded.
3. **`./infra` repo migration is WIP in that separate repo** (nested at `./infra`, its own git
   root, all-untracked): its `platform.toml` was migrated `[ops.vars]`→`[vars]`. Commit it in
   *that* repo, not platform.

## Still OPEN (the 1-by-1 was interrupted here)

- **Item 4 — rename the `latest` strategy → `rolling`. SHIPPED 2026-07-12** (`3ec1785`, unpushed).
  Both sub-Qs decided by operator: **hard rename** (`strategy = "latest"` no longer parses, no
  read-alias) and **strategy value only** — the emitted moving image *tag* stays `:latest`
  (Docker convention Flux follows). `NextName` still returns `"latest"` via the `movingTag`
  const, decoupling the two jobs the old constant conflated (strategy value vs emitted tag).
  Type `Latest`→`Rolling`, file `latest.go`→`rolling.go`, map key, Infra seed, both test
  assertions, testbed toml, live specs + CLAUDE.md all updated; `go build`/`vet`/`test ./...`
  green. Smoke suite ran **UNCHANGED** (no `--commit` needed) — the CHANGED prediction was
  wrong: app Init goldens snapshot `strategy = "timestamp"` (untouched), and infra-basic has
  **no Init smoke test**, so its `rolling` seed isn't snapshotted at the smoke layer. `Infra
  Build` ran the rolling testbed toml through the real binary green (parses end-to-end); the
  infra seed is unit-covered by `init_plan_test.go`. **Item 4 fully closed.** (Pre-existing
  coverage gap, out of scope: no smoke golden captures the infra scaffold's generated toml.)
- **`import_prefix` render teeth (backlog, added 2026-07-12).** Once `import_prefix` lands as a
  seed for the cue.mod module path, consider giving it teeth in the render pass: resolve the
  apps root as `{import_prefix}/apps` rather than the hardcoded `./apps`. Deferred — land the
  setting + module-path decoupling first, revisit render resolution after.
- **Items 5/6 — Flux webhook delivery + anti-regression guard.** Design captured in the prior
  (collapsed) resume; **not built**. Baseline ships a `github`-type Flux Receiver on
  `registry_package` + ingress; platform configures the GitHub repo webhook; `FLUX_HOSTNAME` /
  `PLATFORM_HOSTNAME` join baseline `DefaultVars` (now `[vars]`). Intervals stay short (webhook
  primary, poll = dropped-webhook fallback). Guard: blocking `framework/` test asserting the
  Receiver ships + at-site comment + ADR. Becomes an ADR when built.
- **07-11 audit leftovers** (untouched): releases `else`-after-return, frozen-ADR "builder"
  vocab, dsl blurb naming. **Add:** the 07-11 `terminology-lexicon` + `baseline-dissolves` ADRs
  still say `latest` strategy (and the lexicon still names the deleted `Ops.Tag`) — left as
  frozen provenance, same treatment as the "builder" vocab. Chase only if we decide dated
  decisions should track live vocab.

## Pending school changes (for `ace-school` — carried forward, still not done)

- **`go-coding`** — prefer Go 1.21+ native `GOTOOLCHAIN=go<version>` over the legacy
  `golang.org/dl/go<v>@latest` shim.
- **`ace-docs`** — when pruning `scratch/`/`notes/`, retain files a frozen decision cites as
  provenance; the `scratch/` toll line applies to new files only.
- **`go-coding`** — `debug.ReadBuildInfo()` in `go test` binaries carries no dependency
  versions; seam version-from-buildinfo lookups (`var x = pkg.Fn`) and stub in tests.
- **`ace-docs` (NEW)** — collapse-prior-art pattern: when scratch design files pile up but are
  cited as provenance, consolidate them into one `prior-art.md` digest (a section per source,
  each cross-linked to its live spec/ADR) and repoint citations to the sections — one file, no
  dangling links, provenance preserved.

## Durable-knowledge routing (this save)

- Session work → the 4 commits (code + specs + decisions). CLAUDE.md `project/` block updated
  in-commit. No new repo-fact pending.
- User-MEMORY: one new feedback entry on the "excise the re-seeding artifact, don't re-attend"
  pattern (see [[feedback_excise_relapse_source]]).
- prior-art collapse pattern → school change above.
