<!-- not spec/decision because: session handoff breadcrumb, not durable design -->

# Resume — 2026-07-12 (all pushed, in-slice golden re-record locked)

Entry point for the next session. **Tree is clean; `gh/main` is fully pushed — nothing
outstanding to push.** Prior breadcrumbs collapsed into [`prior-art.md`](prior-art.md).

## What landed this session

- `46b3c61` **docs: lock in-slice smoke-golden re-record into done-definition** (platform
  `CLAUDE.md`, Testing). A slice that moves smoke output isn't done until its golden is
  re-recorded and the diff reviewed *in that same slice* — same tier as `go test`, never a
  session-end batch. Deliberately **platform-only**: the operator declined pushing this into
  the ACE workflow skill or global `CLAUDE.md` (both edits were drafted, reviewed, then
  reverted — the rule belongs in the repo it governs).
- The prior session's 12 commits (infra-as-framework excision + `import_prefix`/module-path
  decoupling + rolling rename) — **all pushed**.

## Smoke goldens — verified UNCHANGED, were never stale

Ran `./test.sh`: **UNCHANGED**, 18 tests, drift-free. The prior breadcrumb's "expect CHANGED
/ stale until re-recorded" was a **wrong prediction — the second such miss** (Item 4 last
session did the same). Why UNCHANGED holds:

- The `import_prefix`/infra-init work already recorded its golden last session — the **Infra
  Init** test snapshots the generated `platform.toml` with `[vars]` baked in.
- App Init goldens cover regular-app scaffolds, which emit **no `[vars]`** (vars are the infra
  baseline's version-pins only) and `strategy = "timestamp"` — untouched by `[ops]`→`[vars]`.

Discipline going forward: **run the suite in-slice and trust UNCHANGED/CHANGED** — don't
predict drift from the shape of a change. `[vars]`/strategy *seed* changes only drift the
smoke layer where a test actually snapshots the scaffold's generated toml (now: infra-init).

## FIRST — next session

Nothing blocking. Pick from the OPEN backlog below.

## Still OPEN (backlog)

- **`import_prefix` render teeth.** Give `import_prefix` teeth in the render pass: resolve the
  apps root as `{import_prefix}/apps` rather than the hardcoded `./apps`. Setting +
  module-path decoupling already shipped; this is the deferred follow-on.
- **Items 5/6 — Flux webhook delivery + anti-regression guard.** Designed (in the collapsed
  prior resume), **not built**. Baseline ships a `github`-type Flux Receiver on
  `registry_package` + ingress; platform configures the GitHub repo webhook; `FLUX_HOSTNAME`
  / `PLATFORM_HOSTNAME` join baseline `[vars]`. Intervals stay short (webhook primary, poll =
  dropped-webhook fallback). Guard: blocking `framework/` test asserting the Receiver ships +
  at-site comment + ADR. Becomes an ADR when built.
- **`./infra` repo migration (separate repo, nested at `./infra`, own git root).** Its
  `platform.toml` was migrated `[ops.vars]`→`[vars]` last session and is uncommitted **in that
  repo**. Commit it there, not in platform.
- **07-11 audit leftovers** (cosmetic, untouched): releases `else`-after-return; frozen-ADR
  "builder" vocab; dsl blurb naming. The 07-11 `terminology-lexicon` + `baseline-dissolves`
  ADRs still say `latest` strategy (lexicon still names the deleted `Ops.Tag`) — left as
  frozen provenance, same as the "builder" vocab. Chase only if dated decisions should track
  live vocab.

## Pending school changes (for `ace-school` — carried forward, still not done)

- **`go-coding`** — prefer Go 1.21+ native `GOTOOLCHAIN=go<version>` over the legacy
  `golang.org/dl/go<v>@latest` shim.
- **`ace-docs`** — when pruning `scratch/`/`notes/`, retain files a frozen decision cites as
  provenance; the `scratch/` toll line applies to new files only.
- **`go-coding`** — `debug.ReadBuildInfo()` in `go test` binaries carries no dependency
  versions; seam version-from-buildinfo lookups (`var x = pkg.Fn`) and stub in tests.
- **`ace-docs`** — collapse-prior-art pattern: when scratch design files pile up but are cited
  as provenance, consolidate them into one `prior-art.md` digest (a section per source, each
  cross-linked to its live spec/ADR) and repoint citations to the sections.
