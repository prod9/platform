# Resume — 2026-06-29 (overnight audit + builders design pass)

> Supersedes [`2026-06-27-resume.md`](2026-06-27-resume.md) as the entry point; that
> note's engine-rework state and queued tasks #2–#6 are still valid and not repeated here.

## What landed this run (afk, local only — NOT pushed)

A full-codebase audit against `general-coding` + `go-coding`, then the start of the #4
builders audit. `main` is now ~25 commits ahead of `gh`.

- **Atomic rubric** — every sentence of both skills as ID'd checkable points:
  [`2026-06-28-coding-audit.md`](2026-06-28-coding-audit.md) (also the findings ledger).
- **24 violations fixed + committed**, all behavior-preserving — `go build/vet/test` green
  and **smoke UNCHANGED**. Commits: `3810ba7` shell · `71ab36d` dsl · `45e43d5` builder ·
  `0c3b9a7` releases/project/bootstrapper · `afcfd30` cmd/internal.
- **15 findings logged for decision, 1 false positive** — dispositions in the audit
  ledger; the decision list (with file:line + recommendation) is in `.afk.log` (gitignored
  — read it before clearing).
- **Builders design pass** —
  [`2026-06-29-builders-reshape-design.md`](2026-06-29-builders-reshape-design.md). Design
  only; not implemented (it changes the `builder.Interface` contract).

> **Update (interactive session, same day):** the review is done. chakrit triaged the 15
> logged findings (10 actioned → `a393a0f`, ⑥ rejected, ⑪–⑭ deferred), resolved the §3
> builders fork, and settled the platform-as-CI architecture; all consolidated into docs
> (`00b7f86`). See **Next** below — it is the current source of truth. Still unpushed.

## Next

1. ~~Triage the 15 logged audit findings.~~ **DONE** (`a393a0f`) — 10 actioned, ⑥ rejected
   (keep release.go bool flags), ⑪–⑭ deferred to the #4 reshape. Behavior-preserving;
   build/vet/test green, smoke UNCHANGED. The four dedup findings still live in `.afk.log`
   / the audit ledger for the reshape.
2. ~~Decide the §3 fork in the builders design note.~~ **RESOLVED** — fork dissolved:
   infra-render is a new `platform/infra` builder returning `*dagger.Container`, 2-phase
   (CUE→k8s render → pack `scratch` image), consumed by Flux as a plain image via
   `OCIRepository` `layerSelector` + Kustomization. Recorded in §0 of
   [`2026-06-29-builders-reshape-design.md`](2026-06-29-builders-reshape-design.md), with
   the full §4 dispositions.
3. **Platform-as-CI architecture is specced** —
   [`2026-06-29-platform-as-ci-design.md`](2026-06-29-platform-as-ci-design.md): module
   split (`core`+`cli` now, `srv`+`webui` later, no `api/` layer), GitHub App auth
   (server-owned, install-vs-user tokens) with zero platform RBAC, repo-prep +
   `/var/cache`, sequencing.
4. **Immediate next coding step: the `core/` + `cli/` module split** — root stays the CLI
   (`go install`/vanity unchanged), `core/` becomes nested module
   `platform.prodigy9.co/core`, `go.work` ties them. Mechanics in the platform-as-CI
   note's "Immediate next" section.
5. **`git push gh main`** — everything is local; waits on your say-so.
6. Prior queued work (#2 `@tag`, #3 appliance, #5 infra `gh` remote, etc.) — see the 06-27
   resume.
