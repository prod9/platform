# Resume — 2026-06-29 (overnight audit + builders design pass)

> Supersedes [`2026-06-27-resume.md`](2026-06-27-resume.md) as the entry point; that note's
> engine-rework state and queued tasks #2–#6 are still valid and not repeated here.

## What landed this run (afk, local only — NOT pushed)

A full-codebase audit against `general-coding` + `go-coding`, then the start of the #4 builders
audit. `main` is now ~25 commits ahead of `gh`.

- **Atomic rubric** — every sentence of both skills as ID'd checkable points:
  [`2026-06-28-coding-audit.md`](2026-06-28-coding-audit.md) (also the findings ledger).
- **24 violations fixed + committed**, all behavior-preserving — `go build/vet/test` green and
  **smoke UNCHANGED**. Commits: `3810ba7` shell · `71ab36d` dsl · `45e43d5` builder ·
  `0c3b9a7` releases/project/bootstrapper · `afcfd30` cmd/internal.
- **15 findings logged for decision, 1 false positive** — dispositions in the audit ledger;
  the decision list (with file:line + recommendation) is in `.afk.log` (gitignored — read it
  before clearing).
- **Builders design pass** — [`2026-06-29-builders-reshape-design.md`](2026-06-29-builders-reshape-design.md).
  Design only; not implemented (it changes the `builder.Interface` contract).

## Next

1. **Triage the 15 logged audit findings** (`.afk.log`). Most are one-word go/no-go. Highlights:
   real bugs (preview.go data race, dockerfile build-args dropped, publish exits 0 on failure,
   listCommits swallows git errors), and `Port *int`→`int` (cross-package).
2. **Decide the §3 fork in the builders design note** — builder.Interface stays container-only
   (infra-render in gitops, recommended) vs generalize output type. Unblocks the reshape; the
   container-builder cleanup slices (§4–5) are independent and ready to start either way.
3. **`git push gh main`** — everything is local; waits on your say-so.
4. Prior queued work (#2 `@tag`, #3 appliance, #5 infra `gh` remote, etc.) — see the 06-27 resume.
