# Resume — 2026-07-09 (logging + `platform clean` landed; smoke go-speed still red)

Entry point for the next session. Supersedes
[`2026-07-08-resume.md`](2026-07-08-resume.md). `main` is at `409f443`, **2 ahead of
`gh` — not yet pushed** (`4ce8ee4` logging, `409f443` clean).

## FIRST — next session starts here

Two slices are committed and green; two things remain open.

1. **Push `main` → `gh`** when ready — carries the logging rework + `platform clean`.
2. **Go build-speed regression** — the live blocker. `go/basic` + `go/workspace` build
   entries blow the deliberate 1-min smoke gate under the full parallel `./test.sh`. The
   gate is a performance assertion; fix the slowness, do not raise the timeout. Deep pass:
   [`2026-07-09-go-build-speed-regression.md`](2026-07-09-go-build-speed-regression.md).
3. **Builder-refactor WIP (Bucket B) still uncommitted** — `builder/*.go`,
   `scaffold/plan.go`, `baseline/files/apps-flux.platform`, plan note
   [`2026-07-08-builder-refactor-plan.md`](2026-07-08-builder-refactor-plan.md). That note's
   Law: **no commits until its final all-encompassing review**. Do not bundle into other
   slices.

## Landed this thread

- **`4ce8ee4` — logging rework.** pterm ripped out; console logger on stdlib `slog` +
  `lmittmann/tint`. `internal/buildlog/enrich.go` wraps `*dagger.ExecError` as a
  `LogValuer` exposing cmd/exit/stderr/stdout — build failures are now self-explaining.
  `internal/buildinfo` splits program output (stdout) from logging (stderr). Net dep
  reduction. Design settled (LogValuer-wrapper is final, user-verified) — **do not reopen.**
  This resolved the old "surface Dagger exec stderr" task (note retired).
- **`409f443` — `platform clean`.** Prunes the local Dagger cache across every discovered
  engine; first-line recovery for "works on a fresh checkout but not here". Root-cause
  write-up:
  [`2026-07-09-pnpm-cache-poison-and-platform-clean.md`](2026-07-09-pnpm-cache-poison-and-platform-clean.md).
  **Do NOT re-propose `apk add nodejs`** — rejected on provisioning-taste grounds, now in
  `CLAUDE.md`.

## Parked / handoff

- **fx handoff** —
  [`2026-07-09-fx-handoff-slog-logvaluer-sink.md`](2026-07-09-fx-handoff-slog-logvaluer-sink.md):
  fxlog's sinks don't resolve `LogValuer`/groups; asks fxlog to own a resolving + color
  console sink so projects converge. **fx agent's remit** — platform's local handler stands
  until fx ships it.
