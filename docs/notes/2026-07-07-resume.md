# Resume — 2026-07-07 (entry point)

> Supersedes [`2026-07-05-resume.md`](2026-07-05-resume.md) as the entry point. Templated
> `init` is done and dogfood-validated; the `deploy` verb + platform-managed environments are
> removed; the whole spec/decision/notes/CLAUDE.md corpus is de-confused to the pull model.

## State (end of 2026-07-06 session)

- **Templated `init` works, zero-fixup** — one `ops init` → `ops render` produces a complete,
  correct infra repo (routing, cred templating, dagger version from linked SDK, flux self-sync
  with inferred `oci://` URL). Validated in a sandbox and on a real `./infra` wipe/re-init.
- **`deploy` verb + `environments` removed.** Deploy = operator commits the app-image ref to the
  infra repo + `ops publish` (server + Flux) or `ops render` + `kubectl apply` (no server).
  Multi-env = infra CUE instances + k8s namespacing; gate = GitHub push perms.
- `./infra` is freshly init'd but **uncommitted** (fresh git repo; old v0.8.4 history gone).

## chakrit's next plan (manual, then report back)

1. **Re-create the entire k8s cluster** from scratch.
2. **Run the platform steps by hand** — the outward dogfood: commit `./infra` → `platform
   publish` the image → commit its digest-pinned ref → `ops render` + `ops publish` → **seed
   flux-sync once** (`kubectl apply k8s/flux`, then `k8s/flux-sync`) so Flux starts pulling.
3. Report back whether the full pull loop comes up clean on a fresh cluster.

## Then — the next major phase: the platform server

Once the manual run confirms the pull loop, the delivery-CLI foundation is complete and the
next arc is the **platform server** (`srv/`) per [`../spec/platform.md`](../spec/platform.md):
in-cluster, API-first (UI + CLI + tofu-provider), identity/RBAC via GitHub, Projects, secret
brokering, kube-token minting. Big phase — its own planning pass.

## Minor deferred cleanups (fold in anytime / into server work)

- `init` end-of-run UX: print a written-files summary + `configure` output (logged in
  [`2026-07-06-templated-init-plan.md`](2026-07-06-templated-init-plan.md)).
- Flatten the `ops` namespace (`ops render` → `render`) — mentioned in passing; not urgent.
