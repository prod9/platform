# Resume — 2026-07-05 (builders reshape: slices 1–2b landed)

> Supersedes [`2026-06-29-resume.md`](2026-06-29-resume.md) as the entry point. That note's
> engine-rework state and queued tasks (06-27 #2–#6) remain valid and are not repeated here.

## What landed this run (local only — NOT pushed; `main` well ahead of `gh`)

Working the #4 builders reshape as a slice sequence, smoke as the drift gate at each step.

- **Slice 1 — docs correction** (`464ab8b`). The `fileutil` rename (§7) was dropped: the
  `builder/fileutil` vs `internal/fileutil` "collision" **never existed in code** — a docs-only
  error. Removed it from the design note, audit ledger (GC-N4 → WONTFIX), and CLAUDE.md's stale
  `internal/` listing (`plog`→`buildlog`; `fileutil`/`dateref`/`timeref` relocated to their real
  homes).
- **Slice 2 — cmd/args/outdir dedup** (`36ab695`). Behavior-preserving: `goAppBin` +
  `pnpmRunArgs` helpers + consts collapsed the copy-pasted resolution. Smoke UNCHANGED.
- **Slice 2b — /platform FHS container layout** (`911e9db`). The big one. Grew out of a
  `cmd`-semantics question → git archaeology proved **nothing was hallucinated**; `cmd`
  (née `BinaryName`) had always named the Go binary, via an unfinished 2023 rename toward
  "command." Resolution: containers now use `/platform/{src,bin,run}` (build / executables-on-PATH
  / runtime-cwd); Go binary = module `Name` at `/platform/bin/<Name>`, run bare on PATH; `cmd`
  is now strictly the runtime command (default = module binary for Go, interpreter for pnpm),
  never the build-output name. `/app`+`/out` retired. Recorded as
  [ADR 2026-07-05](../decisions/2026-07-05-platform-fhs-container-layout.md). Smoke re-recorded
  (only bootstrap `cmd` emission drifted; all builds transparent).

## Slice 3 — `GoBasic` test opt-out: WONTFIX (2026-07-05)

The opt-out is dropped. It rested on a **conflation** of platform's two test suites, and it
contradicts the testing philosophy now recorded as a per-repo Law (CLAUDE.md `### Testing`)
and in the [test-in-build ADR](../decisions/2026-07-05-test-in-build-is-a-hard-gate.md):

- **`go test ./...`** — hermetic unit tests (no docker/network, fresh-clone runnable). Runs
  **inside every image build** (the `Go*` builder gate) and locally. This is the suite in
  `go_basic.go`; verified hermetic — no test imports Dagger, opens a socket, or hits network.
- **`./test.sh`** — blackbox smoke (`chakrit/smoke`), needs docker. Runs **on the host**,
  manually / pre-deploy — the drift detector. Never runs inside the build.

The earlier "in-build test blocks self-build" claim conflated the first with the second.
Platform's `go test ./...` runs fine docker-free, so **self-build was never blocked**.
Test-in-build is a hard, non-configurable gate — blackbox-first, no skip-tests option ever.
Heavier per-project setup, when needed, goes through opt-in **build hooks** (backlog below),
never a gate opt-out.

## Next

1. **Slice 4 — feature non-support explicit** (§3): PNPMStatic/Dockerfile silently drop
   AssetDirs/env; make non-support explicit, don't unify.
2. **Slice 5 — platform self-builder** — **unblocked** (`GoBasic` fits as-is; no slice 3
   dependency).
3. **Slice 6 — `platform/infra` builder** (its own sub-project: CUE→k8s→scratch image + Flux
   `layerSelector` consumption; retire the oras/flux-media-type path in `gitops.Publish`).
4. **`git push gh main`** — everything is local; waits on your say-so.

## Feature backlog

- **Build hooks** (`BeforeTest` / `AfterTest` / `BeforeBuild` / …): opt-in extension points
  for heavier per-project setup (spin up a DB, seed fixtures) around the baked-in build
  steps. Additive setup only — **never** a way to skip the green-tests gate. Design + slice
  TBD.

## Pending non-code follow-ups

- **School PR (ace-school):** tightened `ace/workflow.md` step 15 "Full save + clear" so the
  agent runs `ace-save` immediately without asking (only `/clear` stays the user's call). Edit
  is live in the school clone but uncommitted — propose via `ace-school`.
- **Design note stale:** [`2026-06-29-builders-reshape-design.md`](2026-06-29-builders-reshape-design.md)
  §0 "what survives" still lists the cmd/args separation as pending — slices 2/2b overtook it.
  Reconcile on the next pass.
