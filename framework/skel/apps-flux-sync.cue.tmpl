package apps

import (
	"{{ .ModulePath }}/defaults"
	"prodigy9.co/defs"
	"prodigy9.co/defs/parts"
)

// Flux CRD wrappers, defs-style — scoped here because flux-sync is their only consumer for
// now. Promote to prodigy9.co/defs if another app needs them.
#OCIRepository: Self={
	parts.#Metadata
	#url!:     string
	#tag:      string | *"latest"
	#interval: string | *"10m"
	#secret:   string

	apiVersion: "source.toolkit.fluxcd.io/v1"
	kind:       "OCIRepository"
	spec: {
		interval: Self.#interval
		url:      Self.#url
		ref: tag: Self.#tag
		secretRef: name: Self.#secret
	}
}

#Kustomization: Self={
	parts.#Metadata
	#source!:  string
	#interval: string | *"10m"
	#path:     string | *"./"

	apiVersion: "kustomize.toolkit.fluxcd.io/v1"
	kind:       "Kustomization"
	spec: {
		interval:  Self.#interval
		sourceRef: {kind: "OCIRepository", name: Self.#source}
		path:      Self.#path
		prune:     true
		wait:      true
	}
}

#Receiver: Self={
	parts.#Metadata
	#type:   string | *"github"
	#events: [...string]
	#secret: string
	#resources: [...{apiVersion: string, kind: string, name: string, namespace?: string}]

	apiVersion: "notification.toolkit.fluxcd.io/v1"
	kind:       "Receiver"
	spec: {
		type:   Self.#type
		events: Self.#events
		secretRef: name: Self.#secret
		resources: Self.#resources
	}
}

"flux-sync": {
	_flux_host: string @tag(flux_hostname)

	// Namespace + ghcr.io-pull-secret via the shared #Basics (creds in defaults/basics.cue).
	let nsp = defaults.#Basics & {
		#name: "flux-system"

		[...]
	}

	// HMAC token the Receiver validates GitHub's signature against. Empty placeholder in
	// committed CUE (like the registry creds); put the plaintext token in #data before wiring
	// the GitHub webhook — #Secret base64-encodes it.
	let webhookToken = defs.#Secret & {
		#name: "flux-webhook-token"
		#ns:   nsp.#name
		#data: token: ""
	}

	let source = #OCIRepository & {
		#name:   "infra"
		#ns:     nsp.#name
		#url:    "oci://{{ .ImageBase }}"
		#secret: nsp.#out.pull_secret_name
	}

	let kustomize = #Kustomization & {
		#name:   "infra"
		#ns:     nsp.#name
		#source: source.#name
	}

	// Webhook Receiver — the primary, near-instant reconcile trigger. On a GHCR publish GitHub
	// POSTs the registry_package event here; the Receiver validates the signature and pokes the
	// OCIRepository, whose interval is only the dropped-webhook fallback. Guarded by
	// TestEmbeddedFluxReceiver — do not drop back to poll-only delivery.
	let receiver = #Receiver & {
		#name:   "infra"
		#ns:     nsp.#name
		#events: ["registry_package"]
		#secret: webhookToken.#name
		#resources: [{
			apiVersion: source.apiVersion
			kind:       source.kind
			name:       source.#name
			namespace:  source.#ns
		}]
	}

	// flux-sync owns its hostname (distributed-hosts shape): the ListenerSet
	// attaches to the operator gateway and cert-manager issues the host cert
	// from the #UseCertManager annotation.
	let listeners = defs.#ListenerSet & parts.#UseCertManager & {
		#name:         "flux-listeners"
		#ns:           nsp.#name
		#gateway_name: "nginx"
		#gateway_ns:   "gateway"
		#hosts: [_flux_host]
	}

	// Exposes notification-controller's webhook-receiver service so GitHub can
	// reach it. backendRef is same-namespace (flux-system) → no ReferenceGrant.
	let webhookRoute = defs.#HTTPRoute & {
		#name:             "flux-webhook"
		#ns:               nsp.#name
		#listenerset_name: listeners.#name
		#host:             _flux_host
		#rules: [{
			#service_name: "webhook-receiver",
			#service_port: 80,
			#path: "/hook/"
		}]
	}

	"namespace.yaml":   nsp
	"flux-config.yaml": [source, kustomize]
	"webhook.yaml":     [webhookToken, receiver, listeners, webhookRoute]
}
